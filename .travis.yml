language: generic
sudo: required
services:
  - docker
before_install:
  - docker pull digitalcitadel/digitalcitadel:latest && docker tag digitalcitadel/digitalcitadel:latest digitalcitadel/digitalcitadel:old && docker push digitalcitadel/digitalcitadel:old
  - docker build -t digitalcitadel/digitalcitadel -f Dockerfile-main .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
after_success:
  - docker push digitalcitadel/digitalcitadel